-- Create database if it doesn't exist

CREATE DATABASE IF NOT EXISTS studybuddies_test;

-- Grant all privilidges to root
 GRANT ALL PRIVILEGES ON studybuddies_test.* TO 'root' identified by '1234';

